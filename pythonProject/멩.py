numbers = [3, -20,-3,44]

for var in numbers :
    if var < 0 :
        print(var)

# 해답
#1. 데이터가 존재 (리스트)
# -> vars = [20, -3, 40, -44]

#2. for 반복문을 사용해서 반복 작업이 필요
# -> for num in vars: (변수들을 num에 담아본것)

#3. 데이터가 음수이면 출력
#  -> print(num)